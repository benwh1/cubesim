Possible ffmpeg commands for combining PNG files into a video:

ffmpeg -framerate 30 -i frame_start.png -framerate 30 -i frame%d.png -framerate 30 -i frame_end.png -filter_complex "[0]loop=59:1:0[start]; [2]loop=59:1:0[end]; [start][1][end]concat=n=3:v=1:a=0[out]" -map "[out]" out.mkv

Problem: requires re-encoding because of the use of -filter_complex
