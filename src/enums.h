#ifndef ENUMS_H
#define ENUMS_H

enum Face{
    U, F, R, B, L, D
};

enum Axis{
    X, Y, Z
};

enum Direction{
    Up, Left, Down, Right
};

enum MoveType{
    None, QuarterTurn, HalfTurn, Rotation
};

#endif // ENUMS_H
